---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import DemoSection from '../components/DemoSection.astro';
---

<Layout title="@3plate/graph - Graph Visualization Library">
  <Header />
  
  <main>
    <section class="hero">
      <div class="container">
        <h1>@3plate/graph</h1>
        <p class="tagline">Graph library with stable layout and incremental updates</p>
        <p class="description">
          Inspired by <a href="https://spidermonkey.dev/blog/2025/10/28/iongraph-web.html" target="_blank" rel="noopener">IonGraph Web</a>,
          @3plate/graph provides a powerful graph visualization library with framework-agnostic core
          and wrappers for React, Vue, and Angular.
        </p>
        
        <div class="features-grid">
          <div class="feature">
            <h3>🎯 Stable Layouts</h3>
            <p>Hierarchical layout algorithm keeps nodes in consistent positions even as the graph changes. No more jarring jumps when adding or removing nodes.</p>
          </div>
          <div class="feature">
            <h3>✨ Crisp SVG Rendering</h3>
            <p>Pure SVG output means pixel-perfect rendering at any zoom level. No blurry canvas scaling—your graphs look sharp on retina displays and when exported.</p>
          </div>
          <div class="feature">
            <h3>🔌 Node Ports</h3>
            <p>First-class support for input/output ports on nodes. Edges connect to specific ports with automatic positioning—perfect for dataflow and pipeline visualizations.</p>
          </div>
          <div class="feature">
            <h3>✏️ Editable Mode</h3>
            <p>Built-in support for interactive graph editing. Users can drag nodes, create connections, and modify the graph structure with intuitive controls.</p>
          </div>
          <div class="feature">
            <h3>🔄 Incremental Updates</h3>
            <p>Efficiently update only what changed. Add, remove, or modify nodes and edges without re-rendering the entire graph.</p>
          </div>
          <div class="feature">
            <h3>🎨 Fully Customizable</h3>
            <p>Render nodes with any HTML/CSS content. Style edges with custom markers, colors, and patterns. Apply your own CSS classes and themes.</p>
          </div>
          <div class="feature">
            <h3>↔️ Flexible Orientation</h3>
            <p>Layout graphs in any direction: top-to-bottom, bottom-to-top, left-to-right, or right-to-left. Switch orientations on the fly.</p>
          </div>
          <div class="feature">
            <h3>🗺️ Pan & Zoom</h3>
            <p>Smooth panning and zooming with mouse, touch, and keyboard support. Optional mini-map for navigating large graphs.</p>
          </div>
          <div class="feature">
            <h3>📍 Edge Markers</h3>
            <p>Multiple marker styles for edge endpoints: arrows, circles, diamonds, bars. Mix and match on source and target ends.</p>
          </div>
          <div class="feature">
            <h3>🔌 Framework Agnostic</h3>
            <p>Core library works with vanilla JS. Official wrappers for React, Vue, and Angular integrate seamlessly with your existing app.</p>
          </div>
          <div class="feature">
            <h3>📦 Zero Dependencies</h3>
            <p>Lightweight core with minimal footprint. No heavy charting libraries or complex build requirements.</p>
          </div>
          <div class="feature">
            <h3>🌙 Dark Mode Ready</h3>
            <p>CSS variable-based theming makes it easy to support light and dark modes. Respects user preferences out of the box.</p>
          </div>
        </div>
      </div>
    </section>

    <DemoSection />

    <section class="installation" id="installation">
      <div class="container">
        <h2>Installation</h2>
        <p class="install-intro">Install the core library, or use a framework-specific wrapper:</p>
        
        <div class="install-list">
          <div class="install-row">
            <span class="install-label">Core</span>
            <code class="install-cmd">npm install @3plate/graph-core</code>
          </div>
          <div class="install-row">
            <span class="install-label">React</span>
            <code class="install-cmd">npm install @3plate/graph-react</code>
          </div>
          <div class="install-row">
            <span class="install-label">Vue</span>
            <code class="install-cmd">npm install @3plate/graph-vue</code>
          </div>
          <div class="install-row">
            <span class="install-label">Angular</span>
            <code class="install-cmd">npm install @3plate/graph-angular</code>
          </div>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">
      <p>
        <a href="https://github.com/3plt/graph" target="_blank" rel="noopener">
          View on GitHub
        </a>
      </p>
    </div>
  </footer>
</Layout>

<style>
  main {
    max-width: 100%;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Hero Section */
  .hero {
    padding: 4rem 0;
    background: var(--color-bg);
    border-bottom: 1px solid var(--color-border);
  }

  .hero h1 {
    font-size: 3.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .tagline {
    font-size: 1.5rem;
    color: var(--color-text-muted);
    margin-bottom: 1.5rem;
  }

  .description {
    font-size: 1.125rem;
    color: var(--color-text-muted);
    margin-bottom: 3rem;
    max-width: 700px;
  }

  .description a {
    color: var(--color-primary);
    text-decoration: none;
  }

  .description a:hover {
    text-decoration: underline;
  }

  /* Features Grid */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .feature {
    padding: 1.5rem;
    background: var(--color-bg-secondary);
    border-radius: 0.5rem;
    border: 1px solid var(--color-border);
  }

  .feature h3 {
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
  }

  .feature p {
    color: var(--color-text-muted);
    font-size: 0.95rem;
  }

  /* Installation Section */
  .installation {
    padding: 4rem 0;
    background: var(--color-bg);
  }

  .installation h2 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
  }

  .install-intro {
    color: var(--color-text-muted);
    margin-bottom: 1.5rem;
  }

  .install-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    max-width: 500px;
  }

  .install-row {
    display: flex;
    align-items: center;
    gap: 1rem;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 0.5rem;
    padding: 0.75rem 1rem;
  }

  .install-label {
    font-weight: 600;
    color: var(--color-primary);
    min-width: 70px;
  }

  .install-cmd {
    flex: 1;
    background: var(--color-bg);
    padding: 0.5rem 0.75rem;
    border-radius: 0.25rem;
    font-size: 0.875rem;
    white-space: nowrap;
  }

  /* Footer */
  footer {
    padding: 2rem 0;
    background: var(--color-bg);
    border-top: 1px solid var(--color-border);
    text-align: center;
  }

  footer a {
    color: var(--color-primary);
    text-decoration: none;
    font-weight: 500;
  }

  footer a:hover {
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: 2.5rem;
    }

    .tagline {
      font-size: 1.25rem;
    }

    .features-grid {
      grid-template-columns: 1fr;
    }

    .install-row {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.5rem;
    }

    .install-cmd {
      width: 100%;
    }
  }
</style>
